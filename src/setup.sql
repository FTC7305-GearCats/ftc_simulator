CREATE TABLE blocks(id INTEGER PRIMARY KEY AUTOINCREMENT,
                    dateModifiedMillis INTEGER,
                    enabled BOOLEAN,
                    escapedName TEXT,
                    name TEXT);
